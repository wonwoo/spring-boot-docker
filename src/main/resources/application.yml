spring:
  jpa:
  database: MYSQL
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://mysql/docker
    username: user
    password: password
    driver-class-name: com.mysql.jdbc.Driver
    validation-query: select 1

