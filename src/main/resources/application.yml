spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://mysql/docker
    username: user
    password: password
    driver-class-name: com.mysql.jdbc.Driver
    validation-query: select 1

